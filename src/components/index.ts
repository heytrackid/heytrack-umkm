
/**
 * Components Module
 * Barrel export for all application components
 */

// Note: UI components are now imported directly from @/components/ui/* to avoid unused exports
// Only keep application-specific components here

// Table Components (if used)
// export { DataTable } from './data-table/data-table'

// Layout Components (if used)
// export { AppLayout } from './layout/app-layout'

// Export types if they exist
// export type { ColumnDef } from '@tanstack/react-table'
