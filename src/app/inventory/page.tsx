'use client'

// Import the enhanced inventory page directly
import EnhancedInventoryPage from '@/modules/inventory/components/EnhancedInventoryPage'

export default function InventoryPage() {
  // Use the enhanced inventory page with weighted average cost features
  return <EnhancedInventoryPage />
}
